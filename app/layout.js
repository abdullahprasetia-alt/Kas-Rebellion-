export const metadata = {
  title: "Kas Kelompok",
  description: "Aplikasi pencatatan kas"
};

export default function RootLayout({ children }) {
  return (
    <html>
      <body>{children}</body>
    </html>
  );
}
